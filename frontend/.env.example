VITE_API_URL=http://localhost:4000
# Mapbox: el token se toma automáticamente del backend (MAPBOX_TOKEN en backend/.env
# vía GET /public/maps-config). Descomentar solo para forzar un token propio en build:
# VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here
